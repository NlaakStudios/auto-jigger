/* ==|== nano loader ==================================================== */

//if the role is application then load initialize NanoFW
// (function(){
	// if(M$['isCloudApp']())
		// try {
			// nldr = create('I$NLDR');
		// } catch (e) {
			// M$['err'](this,e,{},Array.prototype.slice.call(arguments, 0));
		// }
	// else
		// M$['pld']();
// })();	